# --- Dati personali / sensibili (NON pubblicare) ---
# SQLite database: contiene tutti i dati di salute reali
fiband.db
# Log con misure di salute reali (pressione, battito, stress)
sync.log
# Dump grezzo del canale bc (BC_DEBUG=1): account + dati salute in chiaro
bc_raw.log
bc_*.log
# Prompt generato per l'analisi AI: contiene dati di salute reali dell'ultimo anno
prompt.txt
# Cattura grezza del traffico Bluetooth: MAC, seriali, dati salute
docs/btsnoop_hci.log
*.btsnoop
btsnoop_hci.log

# --- Ambiente Python (rigenerabile, contiene path della macchina) ---
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/

# --- File di sistema macOS ---
.DS_Store

# --- Config locale con credenziali / indirizzo device ---
.env
!.env.example

# Old git metadata (fresh start backup)
.git.old/
